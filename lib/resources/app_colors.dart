import 'package:flutter/material.dart';

/// Centralized color palette used throughout the application.
///
/// This file provides a consistent set of design tokens for colors,
/// helping maintain a unified visual style across all UI components.
///
/// Each color is defined as a constant to ensure immutability
/// and better performance when used inside widgets.
class AppColors {
  static const colorNightBlack = Color(0xFF121314);

  static const colorGraphite = Color(0xFF1B1C1F);

  static const colorPureWhite = Color(0xFFFFFFFF);

  static const colorSteelGray = Color(0xFFB0B3B8);

  static const colorAzureBlue = Color(0xFF5FAAFF);

  static const colorAzureBlueDark = Color(0xFF4A8DDC);

  static const colorGraphiteLine = Color(0xFF2A2B2E);

  static const colorMutedGray = Color(0xFF7A7F85);

  static const colorSoftRed = Color(0xFFFF7B7B);
}
